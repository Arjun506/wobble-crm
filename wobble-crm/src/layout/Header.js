import React from "react";

function Header() {
    return (
        <div className="header">
            <h2>Wobble One CRM A</h2>
            <button>Logout</button>
        </div>
    );
}

export default Header;